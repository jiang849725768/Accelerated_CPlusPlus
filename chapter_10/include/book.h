#ifndef GUARD_BOOK_H
#define GUARD_BOOK_H

#include <string>
#include <vector>

std::string letter_grade(double grade);

int* pointer_to_dynamic();

char* duplicate_chars(const char* p);

#endif// !GUARD_BOOK_H
